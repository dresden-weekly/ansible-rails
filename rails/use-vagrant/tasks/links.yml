---
- name: Links
  ansible.builtin.file:
    dest: "{{ RAILS_APP_RELEASE_PATH }}/{{ item }}"
    src: "{{ vagrant_mount }}/{{ item }}"
    state: link
  loop: "{{ rails_vagrant_links + rails_vagrant_custom_links | flatten }}"
